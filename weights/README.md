# Model Weights

The trained YOLO checkpoint files are not committed to GitHub because model weights are large binary artifacts.

Place the production checkpoint here before running the app:

```text
weights/household_v2_best.pt
```

The app expects this path in `detector.py`.
